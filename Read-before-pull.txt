virtualenv time-series
cd time-series
. bin/activate
pip install django
django-admin startproject project
Rename project: 
mv project ProjectContainer

Now inside ProjectContainer use git pull to clone

All apps will be inside ProjectContainer folder

Made changes
git add .
git commit
git pull origin master
git push origin master
