virtualenv time-series
cd time-series
. bin/activate
django-admin startproject project
Rename project: ProjectConainer

Now inside ProjectConainer use git pull to clone

All apps will be inside ProjectConainer folder

Made changes
git add .
git commit
git pull origin master
git push origin master
